{
  "name": "xstools-open",
  "private": true,
  "license": "MIT",
  "scripts": {
    "check": "vp check",
    "check-mono": "sherif",
    "check-publish": "pnpm run build && pnpm run check && pnpm run test",
    "changeset-add": "changeset add",
    "changeset-publish": "pnpm run check-publish && changeset publish",
    "clean": "del **/dist **/.build **/.swc **/.turbo **/node_modules !node_modules",
    "commit": "czg",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "prepare": "vp hooks enable",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/node": "24.13.3",
    "@xstools-dev/cli-toolkit": "^0.2.0",
    "@xstools-dev/config-ts": "^0.1.0",
    "typescript": "5.8.3",
    "vite-plus": "^0.3.0"
  },
  "packageManager": "pnpm@12.3.4"
}
